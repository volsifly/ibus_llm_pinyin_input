# Changelog

## 0.0.7 - 2026-08-27

- IBus 增加 GTK4 配置界面，支持多个 LLM 连接配置、通用请求参数、提示词、日志查看和按模型汇总的数据统计。
- 模型配置只保存 Base URL、模型名称和 API Key；Endpoint 固定为 `/chat/completions`，超时、采样和流式参数改为通用配置。
- 默认使用 SSE 流式 `|` 分隔协议请求 18 个候选，候选窗每页显示 9 个，并在候选不足时继续请求。
- system 提示词支持 `{history}` 动态占位符，替换值只包含用逗号连接的历史输入；user 模板继续使用 `{pinyin}`。
- 普通和流式超时统一为 5 秒；流式超时后立即重试一次，第二次超时后使用已有或本地候选。
- 日志改为单文件最大 10 MB、保留一个备份的滚动文件；设置页改为手动刷新，并移除候选窗刷新、缓存命中等高频日志。
- `Delete` 支持跨候选缓存、用户动态词库和领域词库删除所选词。
- LLM 调用统计记录请求次数、成功率、输入/输出 Token 和响应耗时，并按相同模型合计或计算平均值。

## 0.0.6 - 2026-05-14

- 增加候选补充说明：候选列表显示后继续输入不会提交候选，第二次按空格会用这段补充说明调整当前候选。
- 增加候选翻页：候选列表显示后按 `-` 或 `=` 会排除当前候选，请求 LLM 生成下一组候选。

## 0.0.5 - 2026-05-11

- 增加候选词修改模式：候选列表显示后可用 `Ctrl+1..9` 修改对应候选，编辑后按 `Enter` 提交。
- 增加用户动态词库：修正结果会记录修正日志，2 到 12 个汉字的修正词会自动学习，并在后续完整拼音匹配时优先出候选。
- 支持把命中的用户动态词作为 LLM 上下文，提升项目名、产品名、人名和专业词的长期命中率。
- 增加用户动态词库管理 CLI：支持查看、搜索、启用、禁用、删除、清空和导出 `.dict.json`。
- 增强 `Ctrl+数字` 识别，兼容主键盘数字、小键盘数字以及常见 X11/Wayland 物理键码。

## 0.0.4 - 2026-05-11

- 增加 LLM 流式候选更新：模型每输出一个完整候选词，候选框即时追加显示。
- 增强 OpenAI-compatible 流式响应兼容性：
  - 固定按 UTF-8 解码 SSE，修复 LongCat 中文流式输出被误判为无效候选的问题。
  - 兼容 `delta.content`、`message.content` 和顶层 `content` 字段。
  - 流式为空或失败时自动回退到非流式请求。
- 修复候选词不能使用上下键选择的问题，并支持上下循环选择。
- 默认不再向所有服务发送 `thinking` 字段，避免不支持该字段的兼容接口报错。

## 0.0.3 - 2026-05-11

- 修复部分 OpenAI-compatible 服务在 HTTP 长连接复用下可能出现的读超时问题：
  - LLM 请求头增加 `Connection: close`。
  - 请求结束后主动关闭 `requests.Session`。
- 降低默认日志噪音：逐键输入、buffer 追加、焦点切换和模式属性刷新日志改为 debug 级别。

## 0.0.2 - 2026-05-11

- 优化中文模式下的数字输入行为：
  - 未开始输入拼音时，数字键直接放行给当前应用。
  - 已经开始输入拼音后，数字会进入拼音缓冲区，不会中断输入。
  - 候选列表显示时，`1-9` 仍用于选择候选。
- 优化 CapsLock 行为：中文模式下开启 CapsLock 后，英文按键直接放行给当前应用。
- 增加按键状态日志，便于排查 IBus 传入的 CapsLock 与按键状态。
